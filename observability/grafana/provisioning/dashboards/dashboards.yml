# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: 'mockd'
    orgId: 1
    folder: 'mockd'
    folderUid: 'mockd'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
